"""Pure, I/O-free telemetry/report aggregators.

Extracted from ``src.cli`` (Phase 2a maintainability cleanup) so the
self-improvement loop can import the number-crunching without dragging in
Click. ``src.cli`` re-exports each name for back-compat.
"""
